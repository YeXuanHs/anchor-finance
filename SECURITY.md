# 锚点财务 (AnchorFinance) 安全机制

## 一、认证安全

### 1. JWT 令牌安全
- **动态密钥生成**：首次启动时自动生成随机 JWT 密钥并持久化到数据库
- **密码修改令牌失效**：用户修改密码后，所有已签发的令牌立即失效
- **IP 绑定检查**：可选配置，令牌只能从签发时的 IP 地址使用

### 2. 登录保护（移植自 zjmf）
- **双层限流机制**：
  - 第一层：短时间内频繁登录（5次/5分钟）→ "登录过于频繁"
  - 第二层：累计失败次数过多（15次/30分钟）→ "登录已被锁定"
- **IP 维度限流**：基于客户端 IP 进行限流

### 3. 请求频率限制
- **请求间隔**：最小 100ms 间隔
- **10 秒窗口**：最多 50 次请求
- **1 分钟窗口**：最多 200 次请求

## 二、输入验证（移植自 zjmf）

### 用户名
- 4-20 位，只允许字母、数字、下划线

### 密码
- 6-32 位，新密码不能与旧密码相同，确认密码必须匹配

### 手机号 / 邮箱
- 手机号 4-11 位数字；邮箱标准格式

### API Key
- 8-32 位，必须包含大小写字母、数字、特殊字符

### 站点配置
- URL：必须 http/https 开头，无尾部斜杠
- 管理路径：2-32 位字母数字，不能全为数字或字母

## 三、安全防护

### 1. XSS 防护
- 自动检测并阻止 `<script>`、`javascript:` 等 XSS 攻击
- 安全响应头：`X-XSS-Protection`, `X-Content-Type-Options`, `X-Frame-Options`

### 2. SQL 注入防护
- 自动检测 `UNION SELECT`、`DROP TABLE` 等注入攻击
- 使用参数化查询（GORM）

### 3. CORS 配置
- 只允许配置的域名访问，不使用通配符 `*`

### 4. 文件上传验证
- 白名单：只允许安全文件类型（jpg, png, pdf, doc 等）
- 黑名单：禁止可执行文件（exe, bat, sh 等）

## 四、数据安全

### 1. 敏感数据加密
- **AES-256-GCM 加密**：用于加密存储敏感数据（API Key、密码等）
- **bcrypt 密码哈希**：用户密码不可逆加密

### 2. 数据脱敏策略

**重要：脱敏只在前台（用户端）对其他用户的信息进行脱敏，后台管理员始终看到完整信息！**

| 数据类型 | 脱敏规则 | 示例 |
|----------|----------|------|
| 手机号 | 保留前3后4 | `138****5678` |
| 邮箱 | 保留前3和@后内容 | `tes***@example.com` |
| 姓名 | 保留姓 | `张*` |
| 身份证 | 保留前4后4 | `1101**********1234` |
| 银行卡 | 保留后4位 | `***********0123` |
| IP地址 | 保留前两段 | `192.168.*.*` |
| API密钥 | 保留前6后4 | `abcdef****mnop` |

**脱敏路由规则：**

| 接口类型 | 路由前缀 | 用户信息 | IP地址 | 说明 |
|----------|----------|----------|--------|------|
| 后台管理 | `/api/admin/*` | **完整** | **完整** | 管理员可见全部信息 |
| 前台-自己 | `/api/v1/user/*` | **完整** | **完整** | 用户查看自己的信息 |
| 前台-他人 | `/api/v1/users/:id` | **脱敏** | **脱敏** | 查看其他用户公开信息 |
| 前台-日志 | `/api/v1/user/login-logs` | - | **脱敏** | 用户查看自己的登录日志 |

**相关代码文件：**
- `backend/internal/security/security.go` — 脱敏函数（MaskPhone, MaskEmail 等）
- `backend/internal/model/user_masked.go` — 脱敏结构体（MaskedUser, MaskedLoginLog）
- `backend/internal/handler/frontend_user.go` — 前台用户信息 handler
- `backend/internal/handler/user.go` — 后台管理接口（不脱敏）

### 3. 关键词过滤
- 工单敏感关键词自动拦截
- 支持自定义敏感词库

## 五、审计与日志

### 1. 审计日志
- 记录所有 API 请求
- 包含：用户ID、IP、操作、请求数据、响应状态
- 审计日志中敏感数据自动脱敏（密码、token 等）
- 可配置保留天数

### 2. 登录日志
- 记录所有登录尝试，包含：IP、时间、设备信息、结果

### 3. 操作日志
- 关键操作记录，支持导出和清理

## 六、数据库备份

### 1. 自动备份
- 支持定时备份（cron 表达式配置）
- 备份文件自动压缩（gzip）
- 可配置保留天数

### 2. 备份管理
- 手动创建 / 恢复 / 清理备份

## 七、安全配置

所有安全参数均可在后台配置：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| login_block_minutes_1 | 5 | 第一层锁定时间 |
| login_block_minutes_2 | 30 | 第二层锁定时间 |
| login_max_attempts_1 | 5 | 第一层最大尝试 |
| login_max_attempts_2 | 15 | 第二层累计尝试 |
| send_max_daily | 100 | 每日发送限制 |
| request_interval | 100ms | 请求最小间隔 |
| request_max_10s | 50 | 10秒最大请求 |
| request_max_1min | 200 | 1分钟最大请求 |
| audit_log_retention_days | 90 | 审计日志保留天数 |
| xss_filter_enabled | true | XSS过滤开关 |
| sql_filter_enabled | true | SQL注入过滤开关 |
| backup_enabled | false | 自动备份开关 |
| backup_keep_days | 30 | 备份保留天数 |

## 八、安全建议

1. **生产环境必须**：启用 HTTPS、修改默认管理路径、配置强密码策略
2. **定期维护**：审查审计日志、清理过期备份、更新安全配置
3. **应急响应**：发现异常登录立即锁定账号、定期恢复备份测试
